function searchRoute() {
    const start = document.getElementById('start').value;
    const end = document.getElementById('end').value;
    const option = document.getElementById('options').value;

    // Perform mapping and routing based on selected option
    // This is where you would call your mapping API or perform any other actions
    console.log(`Mapping hitchhike from ${start} to ${end} with ${option} option`);
    // Display map and route result on the webpage
    // This is where you would update the map element with the route
}
